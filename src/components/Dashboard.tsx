const Dashboard = () => (
  <section className="dashboard" style={{marginTop: '1.5rem'}}>
    <div className="card" style={{display: 'flex', alignItems: 'center', justifyContent: 'space-between'}}>
      <div>
        <h2 className="title">Config moderation & api management</h2>
        <p className="desc" style={{marginTop: '0.25rem'}}>Coming Soon: Access the admin console to manage rules and APIs.</p>
      </div>

      <div>
        <span className="btn-disabled">Dashboard Coming Soon</span>
      </div>
    </div>
  </section>
);

export default Dashboard;
